<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="SendButtonTheme">
    <item name="background">@drawable/send_button_background</item>
    <item name="android:textColor">@color/white</item>
  </style>

  <style name="TextInputLayoutBubble">
    <item name="counterEnabled">true</item>
    <item name="counterMaxLength">280</item>
    <item name="android:layout_weight">1</item>
    <item name="counterTextColor">@color/colorAccent</item>
    <item name="android:importantForAutofill">no</item>
  </style>

  <style name="TextInputEditTextBubble">
    <item name="android:ems">10</item>
    <item name="android:focusable">true</item>
    <item name="android:importantForAutofill">no</item>
    <item name="android:inputType">textMultiLine|textPersonName</item>
    <item name="android:background">@null</item>
  </style>

</resources>
